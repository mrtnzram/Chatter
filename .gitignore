# macOS Finder metadata
.DS_Store

# uv / Python environment
.venv/
__pycache__/
*.pyc
*.pyo

# User data produced at runtime — not source files
chatter.duckdb
chatter.duckdb.wal
bouts.csv
bouts_audio/

# Jupyter artifacts
.ipynb_checkpoints/

# Claude / local-only project files (not published to the repo)
CLAUDE.md
RELEASE_NOTES.md

# Local-only docs (not published to the repo)
docs/RELEASING.md
docs/SETUP.md
docs/MANUAL.md

# PyInstaller build artifacts (generated locally or in CI)
build/
dist/
chatter_app/Chatter.icns
chatter_app/Chatter.ico
chatter_app/Chatter.iconset/
